export const APP_CONFIG = {
    // Probability of showing the Synthetic image as usage target.
    // 0.6 means 60% chance of Synthetic, 40% Real.
    SYNTHETIC_PROBABILITY: 0.6,

    // Number of warmup cases to require before allowing transition (if enforcing)
    WARMUP_MIN_CASES: 3,
};
